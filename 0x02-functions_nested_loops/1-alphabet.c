#include "main.h"
/**
 * Function - It prints the alphabet, in lowercase, followed by a new line
 */

void print_alphabet(void);
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
		putchar(x);

		_putchar('\n');
}
