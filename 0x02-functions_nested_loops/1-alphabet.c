#include "main.h"

/**
 *print_alphabet - It should be in lowercase, followed by a new line
 */

void print_alphabet(void)
{
	char xy;
	for (xy = 'a'; xy <= 'z'; xy++)
		_putchar(xy);

		_putchar('\n');
}
